==========
WOPISERVER

Build with:
  `docker-compose -f wopiserver.yaml build`

Run with:
  `docker-compose -f wopiserver.yaml up -d`

Inspect the logs
  `docker-compose -f wopiserver.yaml logs -f`

Specs:
  - listening on ports 8443 for HTTPS (internal port is 8443)
  - volume for path `/var/log/wopi`

